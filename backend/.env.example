# PostgreSQL (Psycopg 3 driver). Create DB and user first, or adjust URL.
DATABASE_URL=postgresql+psycopg://adas:adas@localhost:5432/adas_validation?connect_timeout=10

JWT_SECRET=replace-with-openssl-rand-hex-32                
ACCESS_TOKEN_EXPIRE_MINUTES=1440               
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173  
                                                          
#docker compose up -d db

#cd backend
#.\venv\Scripts\activate
#python -m uvicorn app.main:app --host 127.0.0.1 --port 8088 --reload

#cd frontend
#npm run dev 



                                                        